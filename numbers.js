'use strict'
//console.log(0o11);
//let num = 351;
//
//console.log(num.toString(32));




let firstNumber, lustNumber;

while (true) {
firstNumber = +prompt('Введите число');
  if (isNaN(firstNumber) == false) {
    break;
      };
};



while (true) {
  lustNumber = +prompt('Введите число');
  if (isNaN(lustNumber) == false) {
        break;
  } else {
      };
};

console.log('Cymma 2yx 4isel:' + (firstNumber + lustNumber) + '!');





